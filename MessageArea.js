/** Area to display messages
 * 
 */

import { ScrollView } from "react-native";

function MessageArea(){
  return(
    <ScrollView>
      {/* map out all the message cards */}
    </ScrollView>
  )
}

export default MessageArea;